package com.silverback.sentry

import org.junit.Assert.assertEquals
import org.junit.Test

// Placeholder confirming the JVM unit test runner (testDebugUnitTest) is wired.
// Real repository/sync/merge logic tests land in Phase 4 onward.
class ExampleUnitTest {
    @Test
    fun `sanity check`() {
        assertEquals(4, 2 + 2)
    }
}
